<%= form_for(@song) do |f| %>
 <label>Please enter a song name: </label>
  <%= f.text_field(:name) %> <br><br>

  <%= f.collection_select(:artist_id, Artist.all, :id, :name) %><br><br>

  <%= f.collection_select(:genre_id, Genre.all, :id, :name) %> <br><br>

   <%= f.submit %> <br>
<% end %>